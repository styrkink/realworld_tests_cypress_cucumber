{
  "name": "realworld_tests_cypress_cucumber",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "cypress:test": "npx cypress run",
    "cypress:open": "npx cypress open",
    "cypress:run:headed": "npx cypress run --headed",
    "cypress:run:spec": "npx cypress run --spec",
    "cypress:run:chrome": "npx cypress run --chrome",
    "cypress:custom": "npx cypress run --config-file cypress.config.1280x720.js",
    "report": "open cypress/reports/index.html"
    
  },
  "keywords": [],
  "author": "styrkink",
  "license": "ISC",
  "description": "this is the test task for cypress and cucumber tests",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^20.1.1",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.2",
    "@faker-js/faker": "^8.4.1",
    "cypress": "^13.13.1",
    "cypress-mochawesome-reporter": "^3.8.2",
    "glob": "^9.3.5",
    "reflect-metadata": "^0.2.2"
  }
}
